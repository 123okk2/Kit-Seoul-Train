action (findRoute) {
  description (Function to Search Routing Time)
  type(Calculation)
  collect {
    input (selectedStation) {
      type (AllSelectedResult)
      min (Required) max (One)
    }
  }
  output (Result)
}
