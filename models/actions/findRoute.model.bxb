action (findRoute) {
  description (Function to Search Routing Time)
  type(Routing)
  collect {
    input (ToStation) {
      type (toStation)
      min (Required) max (One)
    }
    
    input (DesStation){
      type (desStation)
      min (Required) max (One)
    }
  }
  output (timeOutput)
}
