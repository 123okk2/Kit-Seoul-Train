action (findRoute) {
  description (Function to Search Routing Time)
  type(Calculation)
  collect {
    input (station) {
      type (StationResult)
      min (Required) max (One)
    }
  }
  output (Result)
}
