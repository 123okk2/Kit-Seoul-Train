endpoints {
  action-endpoints {
     action-endpoint (searchStation){
      accepted-inputs (toStation, desStation)
      local-endpoint (StationSelect.js)
    }
   
    action-endpoint (findRoute) {
      accepted-inputs (selectedStation)
      local-endpoint (runAPI.js)
    }
  
  }
}
